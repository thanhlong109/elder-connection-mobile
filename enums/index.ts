export enum ActionStatus {
  APPROVED = 'Đã nhận',
  PENDING = 'Đang chờ',
  DONE = 'Đã xong',
}

export enum MenuStatus {
  SELECTED,
  UN_SELECT,
}

export enum ServiceType {
  SERVICE_4,
  SERVICE_8,
}

export enum ServicePackageType {
  MONTHLY,
  DAILY,
}
