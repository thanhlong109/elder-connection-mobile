import { View, Text } from 'react-native';
import React from 'react';
import { Container } from '~/components/Container';

const home = () => {
  return (
    <Container>
      <Text>home</Text>
    </Container>
  );
};

export default home;
